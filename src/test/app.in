# root window
win root{
    title "Title";
    size (800,600);
}

# button widget
button btn->root{
    title "Title";
    size (800,600);
}

# another button
button enother->root{
    title "Title";
    size (800,600);
}